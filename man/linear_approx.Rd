% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_approx.R
\name{linear_approx}
\alias{linear_approx}
\title{Log-linear approximation for the domestic and world prices}
\usage{
linear_approx(cal, tau)
}
\arguments{
\item{cal}{calibrated model returned by \code{\link[=calibrate_model]{calibrate_model()}}}

\item{tau}{tariff rate for the approximation}
}
\value{
A named list with approximated prices and transmission elasticities.
}
\description{
\code{linear_approx()} implements the closed-form domestic price formula obtained
by log-linearizing the market-clearing condition and solving for \eqn{\log(P_d)}.
}
\details{
\strong{Derivation Recap}
Starting from market clearing in logs:
\eqn{\log(K_{md}) + \eta_{md} \log(P_d) = \log(K_{ms}) + \epsilon_{ms} \log(P_w)}

Using \eqn{\log(P_w) = \log(P_d) - \log(1+tau)} to eliminate \eqn{P_w}:
\eqn{\log(K_{md}) + \eta_{md} \log(P_d) = \log(K_{ms}) + \epsilon_{ms} \log(P_d) - \epsilon_{ms} \log(1+tau)}

Solving for \eqn{\log(P_d)}:
\eqn{\log(P_d) = (\log(K_{md}) - \log(K_{ms}) + \epsilon_{ms} \log(1+tau)) / (\epsilon_{ms} - \eta_{md})}

\strong{Price Transmission Elasticity}
Differentiating w.r.t. \eqn{\log(1+tau)}:
\itemize{
\item \eqn{d \log(P_d) / d \log(1+tau) = \epsilon_{ms} / (\epsilon_{ms} - \eta_{md})}
\item \eqn{d \log(P_w) / d \log(1+tau) = \eta_{md} / (\epsilon_{ms} - \eta_{md})}
}

Interpretation:
\itemize{
\item Small country (\eqn{\epsilon_{ms} \rightarrow \infty}): ratio \eqn{\rightarrow 1} (full pass-through)
\item Large country (finite \eqn{\epsilon_{ms}}): ratio \eqn{< 1} (partial pass-through)
}
}
