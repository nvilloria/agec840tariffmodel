% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve.model.R
\name{solve.model}
\alias{solve.model}
\title{Solve the equilibrium for a given policy configuration}
\usage{
\method{solve}{model}(
  cal,
  tau = cal$tau,
  sp = cal$sp,
  sc = cal$sc,
  quota = NULL,
  rent.domestic = 1
)
}
\arguments{
\item{cal}{calibrated model list of class \code{model} from \code{\link[=calibrate.model]{calibrate.model()}}}

\item{tau}{new tariff (default: calibration benchmark tau)}

\item{sp}{new production subsidy rate (default: calibration sp)}

\item{sc}{new consumption subsidy rate (default: calibration sc)}

\item{quota}{binding import quota in quantities (default NULL = no quota)}

\item{rent.domestic}{share in \code{[0,1]} of quota rents captured by domestic agents
(default 1; set to 0 for a VER-type foreign-rent scenario)}
}
\value{
A named list of class \code{solution} with equilibrium prices, quantities, and fiscal items.
}
\description{
\code{solve.model()} finds the equilibrium given a policy configuration.
It uses base R's \code{uniroot()} to solve the market-clearing condition.
}
\details{
The model operates in two modes:

\strong{Mode A: Elastic import supply (\code{quota = NULL}, default)}
Solves one equation in one unknown (\eqn{P_d}):

\eqn{K_d(P_d(1-sc))^{\eta_d} - K_s(P_d(1+sp))^{\epsilon_s} - K_{ms}(P_d/(1+tau))^{\epsilon_{ms}} = 0}

\strong{Mode B: Binding import quota (\code{quota = positive number})}
Import quantity is fixed at \eqn{M_q}. Two equations solved sequentially:
\enumerate{
\item \eqn{K_d(P_d(1-sc))^{\eta_d} - K_s(P_d(1+sp))^{\epsilon_s} = M_q}
=> domestic price \eqn{P_d} (domestic market clears at quota level)
\item \eqn{K_{ms} P_w^{\epsilon_{ms}} = M_q}
=> world price \eqn{P_w} (foreign supply meets the quota level)
}

The quota rent per unit = \eqn{P_d - P_w} (the scarcity wedge).
Welfare impact depends on who captures the rent (see \code{rent.domestic}).
}
